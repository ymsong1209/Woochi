
public class DarknessBuff : AbnormalBuff
{
    // public override int ApplyBattleStartBuff()
    // {
    //     // Stat stat = buffOwner.BaseStat;
    //     // stat.speed = (int)(stat.speed * 1.5f);
    //     // stat.defense = (int)(stat.defense * 1.5f);
    //     // stat.crit = (int)(stat.crit * 1.5f);
    //     // stat.accuracy = (int)(stat.accuracy * 1.5f);
    //     // stat.evasion = (int)(stat.evasion * 1.5f);
    //     // stat.resist = (int)(stat.resist * 1.5f);
    //     // stat.minStat = (int)(stat.minStat * 1.5f);
    //     // stat.maxStat = (int)(stat.maxStat * 1.5f);
    //     // return base.ApplyBattleStartBuff();
    // }
}
